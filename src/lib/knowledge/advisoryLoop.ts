/**
 * KURLA — note de boucle de réévaluation du diagnostic (peau ET cheveux).
 *
 * Un seul texte, deux pôles : la boucle de recommandation L4 est la même —
 * réévaluation à J+30 sur vos retours et votre journal, recommandation
 * d’évolution si le profil a évolué, sinon aucun push.
 * Texte exact du déclencheur réel : ne pas le paraphraser, ne pas le
 * dupliquer dans les modules peau/cheveux.
 */

export const ADVISORY_LOOP_NOTE =
  'À J+30, KURLA réévalue votre routine à partir de vos retours et de votre journal. '
  + 'Si votre profil a évolué — routine suivie, retours, observations — vous recevrez une recommandation d’évolution avec ses raisons. '
  + 'Sinon, aucun push : votre routine reste valable.';
