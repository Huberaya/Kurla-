node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Bundle serveur genere par le build Vercel (esbuild server.ts)
api/_server.cjs

# Playwright
test-results/
playwright-report/

# État de référence de la production, propre à chaque machine
.kurla-etat-production.json

# État de référence du schéma, propre à chaque machine
.kurla-schema-production.json

# État local de la CLI Supabase (linked project, cache) — propre à chaque machine
supabase/.temp/
